# Filtered by Maven (see pom.xml <resources>): the app reads its own version from here so the
# /api/version endpoint works in every run mode, not only from the packaged jar.
version=${project.version}
